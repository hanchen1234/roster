name: Daily Roster Notification

on:
  schedule:
    - cron: '45 1 * * *'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Run Roster Script
        run: |
          chmod +x standupImpl.sh
          ./standupImpl.sh
